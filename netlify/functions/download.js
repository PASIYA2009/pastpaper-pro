const gh=require('./_github');exports.handler=async(event)=>{try{const id=event.queryStringParameters?.id;if(!id)return {statusCode:400,body:'Missing id'};const p=(await gh.list()).find(x=>x.id===id);if(!p?.download_url)return {statusCode:404,body:'File not found'};return {statusCode:302,headers:{Location:p.download_url,'Cache-Control':'no-store'},body:''}}catch(e){return {statusCode:500,body:e.message}}};
